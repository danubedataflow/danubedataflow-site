#!/usr/bin/env zsh

ICON="src/apple-touch-icon.png"
BUILD="build"

rm -rf ./$BUILD
mkdir ./$BUILD

for i in 16 32 64 128 192
do
    magick $ICON -resize $ix$i ./$BUILD/output-${i}x${i}.ico
done

magick ./$BUILD/output-*.ico src/favicon.ico

rm -rf ./$BUILD
